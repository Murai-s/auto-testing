# Учебный проект по автоматизации тестирования (Selenium + TestNG + Rest-Assured)

Это мой учебный проект, созданный для углубленного изучения и демонстрации навыков в автоматизации тестирования на Java. Фреймворк построен с использованием современных практик и паттернов проектирования.

## 🚀 Используемый стек технологий

*   **Язык:** Java 23
*   **UI-автоматизация:** Selenium WebDriver
*   **API-автоматизация:** Rest-Assured
*   **Тестовый фреймворк:** TestNG
*   **Сборка проекта:** Apache Maven
*   **Вспомогательные библиотеки:** Lombok

## ✨ Ключевые особенности фреймворка

*   **Page Object Model (POM):** Весь UI-код структурирован с использованием паттерна Page Object для улучшения читаемости и поддержки.
*   **Многослойная архитектура:** Четкое разделение на слои для тестов, страниц, API и базовой логики.
*   **"Умные" ожидания:** Реализован `BasePage` с явными ожиданиями (`WebDriverWait`) для всех взаимодействий с элементами, что делает тесты стабильными и надежными.
*   **Разделение UI и API тестов:** Тесты для пользовательского интерфейса и для API находятся в отдельных пакетах для лучшей организации.
*   **BDD-стиль в API-тестах:** Тесты с Rest-Assured написаны в формате `given/when/then` для максимальной наглядности.
*   **Централизованная конфигурация:** Основные параметры (URL, таймауты) вынесены в отдельный класс `TestConfig`.

## ⚙️ Как запустить тесты

1.  Клонировать репозиторий:
    ```bash
    git clone https://github.com/Murai-s/auto-testing.git
    ```
2.  Перейти в директорию проекта:
    ```bash
    cd auto-testing
    ```
3.  Запустить все тесты с помощью Maven:
    ```bash
    mvn test
    ```
4.  Или запустить тесты через файл `testng.xml` в IDE IntelliJ IDEA.
